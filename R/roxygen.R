# general ----
roxy_clean <- function() {
  # nocov start
  'Default is TRUE. Should output be returned as a `tibble` (`TRUE`) or list.'
} # nocov end

roxy_region <- function() {
  # nocov start
  'Region routing value. Platform regions include `"na1"`, `"euw1"`, `"eun1"`, `"kr"`, `"br1"`, `"jp1"`, `"la1"`, `"la2"`, `"oc1"`, `"tr1"`, `"ru"`, `"ph2"`, `"sg2"`, `"th2"`, `"tw2"`, `"vn2"`.'
} # nocov end

roxy_regional <- function() {
  # nocov start
  'Regional routing value. One of `"americas"`, `"asia"`, `"europe"`, `"sea"`, or `"esports"`.'
} # nocov end

roxy_puuid <- function() {
  # nocov start
  'PUUID of the player.'
} # nocov end

roxy_queue <- function() {
  # nocov start
  'Queue type. Such as `"RANKED_SOLO_5x5"` or `"RANKED_FLEX_SR"`.'
} # nocov end

roxy_tier <- function() {
  # nocov start
  'Tier. Such as `"DIAMOND"`, `"MASTER"`, `"GRANDMASTER"`, or `"CHALLENGER"`.'
} # nocov end

roxy_division <- function() {
  # nocov start
  'Division. Such as `"I"`, `"II"`, `"III"`, or `"IV"`.'
} # nocov end

roxy_game_name <- function() {
  # nocov start
  'Game name of the Riot account.'
} # nocov end

roxy_tag_line <- function() {
  # nocov start
  'Tag line of the Riot account.'
} # nocov end

roxy_match_id <- function() {
  # nocov start
  'Match ID.'
} # nocov end

roxy_champion_id <- function() {
  # nocov start
  'Champion ID.'
} # nocov end
